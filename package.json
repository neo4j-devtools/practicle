{
  "name": "release-notes",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "jest",
    "generate": "babel-node --presets=env src/generate.js",
    "precommit": "lint-staged"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "lint-staged": {
    "*.{js,jsx,json,css}": [
      "prettier --write",
      "git add"
    ]
  },
  "author": "Neo4j Inc.",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-jest": "^23.4.2",
    "babel-preset-env": "^1.7.0",
    "husky": "^0.14.3",
    "jest": "^23.4.2",
    "lint-staged": "^7.2.0",
    "prettier": "^1.14.0",
    "yarn": "^1.9.4"
  }
}
